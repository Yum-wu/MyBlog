---
paths:
  - "client/src/content/posts/**/*.md"
---

# Markdown 文章规范

- slug 必须与文件名一致（不含 .md）
- lang 字段：`zh`（根目录）或 `en`（en/ 子目录）
- 用 `lang:` 不用 `language:`（已标准化）
- 中英文各一份，文件名相同，目录不同
- frontmatter 用 YAML，字段名用英文
